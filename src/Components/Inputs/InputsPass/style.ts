import styled from "styled-components";

export const StyledDivPassword = styled.div`
    display: flex;
    position: relative;

    button {
        position: absolute;
        top: 35%;
        right: 10px;
        border: none;
    }
`